name = "viewsource"
compatibility_date = "2026-09-18"

# only public/ is served; .git, docs/ and config sit outside the assets root
[assets]
directory = "public"

# apex custom domain; wrangler creates the DNS record on deploy
[[routes]]
pattern = "viewsource.cc"
custom_domain = true
